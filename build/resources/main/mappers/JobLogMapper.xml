<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.quartzui.mapper.JobLogMapper">
    <insert id="insertLog">
        INSERT INTO job_logs(job, step, status, message, created_at)
        VALUES(#{job}, #{step}, #{status}, #{message}, CURRENT_TIMESTAMP)
    </insert>
</mapper>


